managed_components
build/
.DS_Store
/sdkconfig.old

# Keep Squareline GUI save files only
squareline/
!squareline/SquarelineGUI.sll
!squareline/SquarelineGUI.spj

# Secret credentials (wifi and MQTT)
main/include/secret_credentials.h